# Dedicated Hydra config for main_vec.py.
# This config tree is intentionally separate from new_rl/config.
defaults:
  - env: crowdsim
  - model: default
  - trainer: ppo
  - _self_

render: false
model_folder: default
run_name: null
device: cuda
seed: 0
eval_seed: 100

hydra:
  job:
    chdir: false
